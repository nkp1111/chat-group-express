<nav class="navbar">

  <div class="navbar-profile d-flex gap-2 align-items-center">
     <!-- user image  -->
    <div>
      <img src="<%= user?.image?.url || "https://www.gravatar.com/avatar/" %>" alt="avatar" />
    </div>

     <!-- username  -->
     <div class="d-none current-user-name">
       <%=  user?.username || "None" %>
      </div>
      <!-- username  -->
      <div>
        <%= (user?.name && user.name[0].toUpperCase() + user.name.slice(1,)) || user?.username || "None" %>
      </div>

    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <button class="dropdown-toggle" id="navbarDropdown" data-bs-toggle="dropdown" aria-expanded="false"></button>

        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
          <li>
            <a class="dropdown-item" href="/profile">
              <i class="fa-solid fa-user"></i>
               My Profile
            </a>
          </li>
          <li>
            <a class="dropdown-item" href="/">
              <i class="fa fa-duotone fa-user-group"></i>
              Group Chat
            </a>
          </li>

          <li><hr class="dropdown-divider" /></li>

          <li class="logout-item">
            <a href="/user/logout" class="dropdown-item text-danger">
              <i class="fa-solid fa-right-from-bracket text-danger"></i>
              Log Out
            </a>
          </li>
        </ul>

      </li>
    </ul>
  </div>
</nav>